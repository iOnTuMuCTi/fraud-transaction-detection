# Отчёт о подготовке проекта

## Проверено

- Статья очищена от вставок, удалений и изменений форматирования в режиме рецензирования.
- DOCX повторно отрендерен; визуально проверены все 9 страниц.
- Подготовлена PDF-версия статьи.
- Ноутбук очищен от сохранённых выводов и прерванного Optuna-запуска.
- Удалены `%pip`, `kagglehub` и абсолютные пути Kaggle.
- Загрузка данных переведена на локальный файл или переменную `FRAUD_DATA_PATH`.
- Препроцессоры включены в ML-пайплайны внутри кросс-валидации, что устраняет утечку данных между фолдами.
- Добавлены воспроизводимые seed, настройка числа Optuna-испытаний и проверка схемы CSV.
- Все code cells ноутбука проходят синтаксическую компиляцию.

## Ограничение проверки

Полное повторное обучение не выполнялось: оригинальный CSV сейчас не доступен публично и не был приложен к исходным материалам. Поэтому ноутбук не содержит неподтверждённых метрик; он рассчитает их после добавления исходного файла.

